//a function that asks the user for their date of birth
let dob = prompt("Please enter your date of birth (YYYY-MM-DD):");
console.log("Your date of birth is: " + dob);
return dob;
// Call the function
getDateOfBirth();